# Copy to .env.local and adjust if the backend isn't on its default dev port.
# Base URL of the FastAPI backend (see backend/AGENTS.md / uvicorn app.main:app --reload).
NEXT_PUBLIC_API_URL=http://localhost:8000
